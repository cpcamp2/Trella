<h2>Create a Task</h2>

<% if @task.errors.any? %>
<h4>Something Went Wrong:</h4> 
  <ul>
    <% @task.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= simple_form_for @task do |f| %>
  <%= f.input :content %>
  <%= f.button :button %>
<% end %>